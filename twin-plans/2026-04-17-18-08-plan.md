# Twin Development Plan
Generated: 2026-04-17
Task: Implementar Sprints 1 e 2 do Plano de Arquitetura Agente V2
Quality Level: pragmatic

## Análise Técnica

**Sprint 1 — Tool Gating:**
- Ponto de intervenção: `src/nodes/generateAIResponse.ts` ~linha 600, spread das 4 calendar tools
- Condição atual verifica apenas `botEnabled !== false && calendarEnabled`
- Flag nova `requireSlotsForCalendar` + `calendarRequiredSlots[]` em `ClientConfig.settings`
- Mapeamento via `src/lib/config.ts` ~linha 254 (padrão snake_case → camelCase)
- `ContactMetadata` já tem `[key: string]` — extensível sem quebra de interface

**Sprint 2 — PolicyStateResolver + StatePersistence:**
- Tabela `conversations` não tem `policy_context` — migration necessária
- Criar módulo `src/lib/policy-engine/` com tipos, slot-manager, state-resolver
- Integrar em `chatbotFlow.ts` APENAS para logging (não muda comportamento)
- Persistir `policy_context` via Supabase client ao final de `generateAIResponse.ts`

## Plano de Implementação

### Arquivos a Criar:

- `supabase/migrations/TIMESTAMP_add_policy_context_to_conversations.sql` — ALTER TABLE conversations ADD COLUMN IF NOT EXISTS policy_context JSONB DEFAULT NULL + index
- `src/lib/policy-engine/types.ts` — PolicyState, PolicyContext, SlotSchema, PolicyResolution
- `src/lib/policy-engine/slot-manager.ts` — checkSlotsFilled(), getPendingSlots() (funções puras)
- `src/lib/policy-engine/state-resolver.ts` — resolvePolicy() baseado nos slots e config
- `src/lib/policy-engine/index.ts` — re-exports

### Arquivos a Modificar:

- `src/lib/types.ts` (~linha 505) — adicionar `requireSlotsForCalendar?: boolean` e `calendarRequiredSlots?: string[]` ao objeto `settings` do `ClientConfig`
- `src/lib/config.ts` (~linha 254) — mapear `require_slots_for_calendar` e `calendar_required_slots` do `client.settings`
- `src/nodes/generateAIResponse.ts` (~linha 600) — (Sprint 1) condição `slotsAreFilled` antes das calendar tools; (Sprint 2) persistir `policy_context` após resposta LLM
- `src/flows/chatbotFlow.ts` — chamar `resolvePolicy` após nó `checkOrCreateCustomer`, logar resultado, não alterar comportamento
- `docs/tables/tabelas.md` — documentar nova coluna `policy_context JSONB` na tabela `conversations`

### Ordem de Implementação:

1. Modificar `src/lib/types.ts` — contrato de tipos, base para tudo
2. Modificar `src/lib/config.ts` — mapear as flags do banco
3. Modificar `generateAIResponse.ts` (Sprint 1 apenas) — slotsAreFilled + gating de tools
4. Criar migration e aplicar com `supabase migration new + db push`
5. Criar `src/lib/policy-engine/types.ts`
6. Criar `src/lib/policy-engine/slot-manager.ts`
7. Criar `src/lib/policy-engine/state-resolver.ts`
8. Criar `src/lib/policy-engine/index.ts`
9. Modificar `chatbotFlow.ts` — integrar resolvePolicy para logging
10. Modificar `generateAIResponse.ts` (Sprint 2) — persistir policy_context
11. Atualizar `docs/tables/tabelas.md`

### Riscos Técnicos:

- **Race condition na persistência**: garantir UPDATE com `client_id` + `phone` nunca só `phone`
- **conversations sem linha no turno 1**: verificar se upsert é necessário; usar `upsert` com `onConflict` se linha pode não existir
- **contactMetadata null no turno 1**: slot-manager deve tratar metadata null/undefined, retornar `pendingSlots = required` sem lançar erro
- **Flag default false**: correto — clientes existentes não são afetados

## Próximo Passo
Para implementar este plano, digite: ok, continue, ou approve
Para cancelar, digite: cancel ou inicie uma nova tarefa
